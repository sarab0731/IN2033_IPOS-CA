# Auto detect text files and perform LF normalization
* text=auto

# Shell scripts must always use LF — CRLF endings break bash inside Linux containers
*.sh text eol=lf

# Dockerfile also requires LF
Dockerfile text eol=lf
